# frozen_string_literal: true

# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
    # Options for SimH Vagrant provider.
    config.vm.provider :simh do |simh|
        
    end
  end